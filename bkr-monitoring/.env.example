# Copy to .env and fill in your values
# .env is gitignored and auto-loaded by docker compose

# Required for LLM diagnosis (optional — system works without it using heuristics)
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
